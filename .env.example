# Path to the directory where log files will be stored (relative or absolute)
LOG_DIR="./logs"

# Name of the main log file
LOG_FILE="sys_audit.log"

# Enable verbose mode to duplicate log output directly to the terminal (true/false)
VERBOSE="false"